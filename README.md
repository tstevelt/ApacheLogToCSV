# ApacheLogToCSV
Parse Apache access_log into pipe delimited file. C program.

* No dependancies on any other libraries
* Create pipe delimited file from Apache access_log.  Log file may contain embedded commas, so (despite the program name) the pipe signs are used.
* Output file can be loaded into your favorite spreadsheet program.
* Spreadsheet can produce much more sophisticated analysis than grep or awk.
